require("conf.custom.keybindings") -- Load custom keybindings
require("conf.custom.autostart") -- Load autostart applications and services
require("conf.custom.gesture") -- Load gesture configuration
require("conf.custom.style") -- Load custom styles
require("conf.custom.keyboard") -- Load custom keyboard configuration